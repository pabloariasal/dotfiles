#!/bin/sh

set -e

function list_files()
{
    fd -e jpg . "$1"
}

wallpaper=$({ list_files "${HOME}"/Dropbox/wallpapers; list_files "${HOME}/wallpapers"; } | shuf | head -n 1)
echo "$wallpaper"
feh --bg-fill "$wallpaper"
notify-send "New Wallpaper: $wallpaper"
