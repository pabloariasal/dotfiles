#!/bin/bash

set -e

drafts_dir=/tmp/drafts
mkdir -p "$drafts_dir"
trunk=`date '+%Y-%m-%d-%H-%M'`-`uuidgen -t | head -c 5`
tmp_file=${drafts_dir}/"${trunk}.$1"
echo $tmp_file

text-scratchpad "$tmp_file"
