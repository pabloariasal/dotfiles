#!/bin/bash

INCREMENT=5
case "$1" in
    up)
        vol=$(amixer -D pulse set Master "$INCREMENT"%+ unmute)
        ;;
    down)
        vol=$(amixer -D pulse set Master "$INCREMENT"%- unmute)
        ;;
    toggle)
        vol=$(amixer -D pulse set Master toggle)
        ;;
esac

notify-volume "$vol"
