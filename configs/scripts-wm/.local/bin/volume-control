#!/bin/bash

INCREMENT=5
case "$1" in
    up)
        vol=$(amixer set Master "$INCREMENT"%+ unmute)
        ;;
    down)
        vol=$(amixer set Master "$INCREMENT"%- unmute)
        ;;
    toggle)
        vol=$(amixer set Master toggle)
        ;;
esac

notify-volume "$vol"
