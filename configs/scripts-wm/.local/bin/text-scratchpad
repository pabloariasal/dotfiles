#!/bin/bash

set -e

alacritty --class="__text_scratchpad" -e $SHELL -lc "sleep 0.1 && nvim -c startinsert -c 'setlocal spell' ${1}" && \
xclip -selection clipboard < $1 && \
xdotool type "$(cat $1)"
